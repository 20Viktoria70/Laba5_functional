# Вектори 2D (Vector2) і базові операції
# Створити ADT для векторів у 2D (через добуток примітивних типів). Реалізувати операції норми, скалярний, векторний добуток, змішаний добуток.

from dataclasses import dataclass
import math

@dataclass(frozen=True)
class Vector2:
    x: float
    y: float


def norm(v: Vector2) -> float:
    match v:
        case Vector2(x, y):
            return math.sqrt(x*x + y*y)


def dot(a: Vector2, b: Vector2) -> float:
    match a, b:
        case Vector2(ax, ay), Vector2(bx, by):
            return ax*bx + ay*by


def cross(a: Vector2, b: Vector2) -> float:
    """2D cross product → скаляр"""
    match a, b:
        case Vector2(ax, ay), Vector2(bx, by):
            return ax*by - ay*bx


def mixed(a: Vector2, b: Vector2, c: Vector2) -> float:
    """
    Змішаний добуток у 2D = cross(b-a, c-a)
    Це подвійна площа трикутника ABC
    """
    match a, b, c:
        case Vector2(ax, ay), Vector2(bx, by), Vector2(cx, cy):
            return (bx-ax)*(cy-ay) - (by-ay)*(cx-ax)
